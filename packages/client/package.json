{
  "name": "@opra/client",
  "version": "1.30.1",
  "description": "Opra Client package",
  "author": "Panates",
  "license": "MIT",
  "homepage": "https://www.oprajs.com",
  "scripts": {
    "compile": "tsc",
    "prebuild": "npm run check && npm run lint && npm run clean",
    "build": "tsc -b tsconfig-build.json && node esbuild.mjs",
    "postbuild": "node ../../support/postbuild.cjs",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --max-warnings=0 --fix",
    "format": "prettier . --write --log-level=warn",
    "check": "dpdm -T --no-warning --no-tree ./src/index.ts",
    "test": "mocha",
    "clean": "npm run clean:src && npm run clean:test && npm run clean:build && npm run clean:cover",
    "clean:src": "ts-cleanup -s src --all",
    "clean:test": "ts-cleanup -s test --all",
    "clean:build": "rimraf build *.tsbuildinfo",
    "clean:cover": "rimraf ../../coverage/client"
  },
  "dependencies": {
    "@browsery/http-parser": "^0.5.10-r3",
    "@browsery/multipart-stream": "^2.0.1-r2",
    "@jsopen/objects": "^2.3.1",
    "accepts": "^1.3.8",
    "multipasta": "^0.2.8",
    "tslib": "^2.8.1",
    "uid": "^2.0.2"
  },
  "peerDependencies": {
    "@opra/common": "^1.30.1",
    "rxjs": "^7.0.0 || ^8.0.0"
  },
  "devDependencies": {
    "@browsery/type-is": "^2.0.2-r2",
    "body-parser": "^2.3.0",
    "expect": "^30.5.1",
    "express": "^5.2.1",
    "ts-gems": "^4.0.3"
  },
  "type": "module",
  "module": "./index.js",
  "browser": "./browser.js",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "require": {
        "types": "./index.d.ts",
        "default": "./index.js"
      },
      "browser": {
        "types": "./index.d.ts",
        "default": "./browser.js"
      },
      "default": "./index.js"
    },
    "./package.json": "./package.json"
  },
  "engines": {
    "node": ">=20.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/panates/opra.git",
    "directory": "packages/client"
  },
  "sideEffects": false,
  "keywords": [
    "opra",
    "node",
    "client",
    "browser",
    "http",
    "request",
    "fetch"
  ],
  "publishConfig": {
    "access": "public"
  }
}